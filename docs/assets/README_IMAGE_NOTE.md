# README Image Note

The README hero should use the fitted PNG asset when pushed:

```text
docs/assets/case_elyria_boundary_layer_hero_fit.png
```

This image is sized for repository front-page display with safe padding around the poster so the footer and right edge are not clipped by README rendering.
